/**
 * Configuration for the Options Visualizer frontend
 */
const CONFIG = {
    // Backend API URL - using relative path for combined app
    BACKEND_URL: '',
    
    // Polling interval in milliseconds
    POLLING_INTERVAL: 5000,
    
    // Default ticker
    DEFAULT_TICKER: 'SPY'
}; 